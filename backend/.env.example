# ── SentCrypto Backend — Variáveis de Ambiente ──────────
# Copie este arquivo como .env e preencha os valores

# (Opcional) Twitter API v2 — necessário apenas se NÃO usar cookies
# TWITTER_BEARER_TOKEN=seu_token_aqui

# O banco de dados SQLite é criado automaticamente em ./dados.db
